[build]
builder = "NIXPACKS"
buildCommand = "pip install -r spaza-pwa/requirements.txt"

[deploy]
startCommand = "cd spaza-pwa && python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
